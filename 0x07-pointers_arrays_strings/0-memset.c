#include "main.h"

/**
 * _memset - ok
 * @n: ok
 * @s: ok
 * @b: ok
 * Return: a piontrer
 */

char *_memset(char *s, char b, unsigned int n)
{
	unsigned int = 0;

	while (i < 0)
	{
		s[i] = b;
		i++
	}
	return (s);
}
