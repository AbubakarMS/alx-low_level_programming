#include "main.h"

/**
 * _memcpy - ok
 * @s: ok
 * @b :ok
 * @n : ok
 * Return: ko
 */

har *_memset(char *s, char b, unsigned int n)
{
	unsigned int i = 0;

	while (i < n)
	{
		dest[i] = src[i];
		i++;
	}
	return (dest);
}
